import {render, screen} from '@testing-library/react'
import '@testing-library/jest-dom'
import Upload from './Upload';

describe('Upload', () => {
  test('renders Upload component', async () => {
    // component not yet defined
  })
});
