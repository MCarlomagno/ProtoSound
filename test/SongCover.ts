describe("SongCover", function () {
  // test case: 
  // 1. create a SongCover instance
  // 2. call the multiMint() method
  // 3. check the owner of the tokens calling ownerCollections()
  // 4. call ownerCollection() method with owner and collectionId
  // 6. start-end range should match with minted token ids.

  // test case:
  // 1. create SongCover instance
  // 2. call the multiMint() method with 3 urls
  // 3. transfer 3 times successfully from one account to another
  // 4. transfer 1 more time should revert.
});
