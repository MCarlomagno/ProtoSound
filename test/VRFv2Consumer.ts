describe("VRFv2Consumer", function () {
  // test case: 
  // 1. create a VRFv2Consumer instance
  // 2. call the requestRandomWords() method and take requestId
  // 3. call getRequestStatus() with requestId.
  // 4. check the status of the request.
  // 5. if request fullfilled, check randomWord.
});