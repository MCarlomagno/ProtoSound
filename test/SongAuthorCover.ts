describe("SongAuthorCover", function () {
  // test case: 
  // 1. create a SongAuthorCover instance
  // 2. call safeMint() method passing a valid address and uri
  // 3. call ownerOf() method passing 0 as token id
  // 4. returned value should match the address and uri passed to safeMint()

  // test case: 
  // 1. create a SongAuthorCover instance
  // 2. call safeMint() method passing a valid address and uri
  // 3. call transferFrom() method passing a valid address and token id.
  // 4. should trow an error
});